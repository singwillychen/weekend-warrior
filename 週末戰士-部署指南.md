# 週末戰士平台 — QNAP 部署指南

**設備：** QNAP TS-932PX (ARM64) + Container Station  
**架構：** 本地 NAS 主力 + Cloudflare Tunnel 遠端存取  
**最後更新：** 2026-06-06

---

## 目錄

1. [資料夾結構建立](#1-資料夾結構建立)
2. [QNAP Container Station 設定](#2-qnap-container-station-設定)
3. [Cloudflare Tunnel 申請與設定](#3-cloudflare-tunnel-申請與設定)
4. [啟動系統](#4-啟動系統)
5. [備份策略](#5-備份策略)
6. [日常維護指令](#6-日常維護指令)
7. [Phase 2 升級路線](#7-phase-2-升級路線)

---

## 1. 資料夾結構建立

在 QNAP File Station 建立以下結構（建議放在一顆**專用硬碟**的 Shared Folder）：

```
/share/weekend-warrior/        ← 根目錄（Shared Folder 名稱）
├── docker-compose.yml         ← 主設定檔
├── .env                       ← 環境變數（機密，勿分享）
├── nginx/
│   └── nginx.conf             ← Nginx 設定
├── app/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── main.py                ← FastAPI 主程式（後續開發）
└── data/
    ├── postgres/              ← DB 資料（自動建立）
    ├── uploads/               ← 上傳的 CSV、圖片
    └── exports/               ← 輸出的 PDF、Word
```

**操作步驟：**

1. 開啟 QNAP File Station
2. 選一顆硬碟 → 右鍵 → 建立 Shared Folder → 命名 `weekend-warrior`
3. 在資料夾內依上方結構建立子目錄
4. 將本指南附件中的設定檔上傳到對應位置

---

## 2. QNAP Container Station 設定

### 2.1 確認 Container Station 已安裝

1. QTS 管理介面 → App Center → 搜尋 **Container Station**
2. 若未安裝，點擊安裝（免費）
3. 版本確認：需 3.0 以上（支援 Docker Compose v2）

### 2.2 開啟 SSH 連線（用指令操作更方便）

1. QTS → 控制台 → 網路與檔案服務 → Telnet/SSH
2. 勾選「啟用 SSH 連線」→ 套用
3. 從你的電腦連入：
   ```bash
   ssh 你的NAS帳號@114.45.3.228
   ```

### 2.3 確認 docker-compose 可用

SSH 連入後執行：
```bash
docker compose version
# 應該看到 Docker Compose version v2.x.x
```

若顯示 `command not found`，在 Container Station 介面 → 設定 → 確認版本。

---

## 3. Cloudflare Tunnel 申請與設定

Cloudflare Tunnel 讓你不需要固定 IP，從任何地方都能安全連到 NAS，完全免費。

### 3.1 申請 Cloudflare 帳號與 Tunnel

1. 前往 [https://dash.cloudflare.com](https://dash.cloudflare.com) 申請免費帳號
2. 左側選單 → **Zero Trust** → **Networks** → **Tunnels**
3. 點擊 **Create a tunnel** → 選 **Cloudflared**
4. Tunnel 名稱輸入：`weekend-warrior-nas`
5. 下一頁會出現一段 Token，格式如：
   ```
   eyJhIjoixxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx...
   ```
6. 複製這個 Token，貼到 `.env` 檔的 `CLOUDFLARE_TUNNEL_TOKEN=` 後面

### 3.2 設定 Tunnel 路由（Public Hostname）

在 Cloudflare Tunnel 設定頁：

| 欄位 | 填入值 |
|------|--------|
| Subdomain | `ww`（或你喜歡的名稱） |
| Domain | 你的網域（需先加入 Cloudflare）|
| Type | HTTP |
| URL | `nginx:80` |

> **沒有自己的網域？** 可以在 Cloudflare 免費取得一個 `.workers.dev` 子網域，或購買網域（約 NT$400/年）。

完成後，你會得到一個網址，例如：`https://ww.yourdomain.com` — 這就是你從外面連進系統的入口。

---

## 4. 啟動系統

### 4.1 準備 .env 檔案

```bash
# SSH 連入 NAS 後
cd /share/weekend-warrior

# 複製範本並編輯
cp .env.template .env
vi .env   # 或用 nano .env
```

填入以下必填欄位：
- `DB_PASSWORD` — 設定一個強密碼（16字元以上，含大小寫+數字+符號）
- `SECRET_KEY` — 隨機字串，可用此指令產生：`openssl rand -hex 32`
- `CLOUDFLARE_TUNNEL_TOKEN` — 上一步複製的 Token
- `FX_API_KEY` — 前往 openexchangerates.org 免費申請

### 4.2 第一次啟動

```bash
cd /share/weekend-warrior

# 建置並啟動所有容器（背景執行）
docker compose up -d --build

# 確認全部容器正常運作
docker compose ps
```

正常狀態應顯示：

```
NAME            STATUS          PORTS
ww_postgres     Up (healthy)    5432/tcp
ww_backend      Up              8000/tcp
ww_nginx        Up              0.0.0.0:8088->80/tcp
ww_tunnel       Up
```

### 4.3 本機測試

在同一個網路內，開瀏覽器輸入：
```
http://114.45.3.228:8088
```
應該看到系統登入頁面。

遠端（從外面）：
```
https://ww.yourdomain.com
```

---

## 5. 備份策略

### 5.1 資料庫自動備份（每日）

在 QNAP → 工作排程器 → 建立排程工作，每天凌晨 2:00 執行：

```bash
docker exec ww_postgres pg_dump -U ww_admin weekend_warrior \
  | gzip > /share/backup/ww_db_$(date +%Y%m%d).sql.gz

# 只保留最近 30 天備份
find /share/backup/ -name "ww_db_*.sql.gz" -mtime +30 -delete
```

### 5.2 雲端備份（Backblaze B2，約 NT$150/月）

1. 申請 Backblaze 帳號 → 建立 Bucket `weekend-warrior-backup`
2. QNAP App Center → 安裝 **Hybrid Backup Sync**
3. 設定每日將 `/share/backup/` 同步到 B2
4. 費用：每 GB NT$0.6，通常 10GB 以內

### 5.3 還原資料庫（緊急情況）

```bash
# 從備份還原
gunzip -c /share/backup/ww_db_20260101.sql.gz \
  | docker exec -i ww_postgres psql -U ww_admin weekend_warrior
```

---

## 6. 日常維護指令

```bash
# 查看所有容器狀態
docker compose ps

# 查看後端 log（即時）
docker compose logs -f app

# 重啟某個容器
docker compose restart app

# 更新程式碼後重新部署
docker compose up -d --build app

# 停止全部容器
docker compose down

# 資料庫進入互動介面
docker exec -it ww_postgres psql -U ww_admin weekend_warrior
```

---

## 7. Phase 2 升級路線

當系統穩定後（預計 3–6 個月），可考慮：

| 升級項目 | 說明 | 效益 |
|---------|------|------|
| RAID 1 設定 | 把資料庫磁碟改為鏡像 | 防止單顆硬碟壞掉資料遺失 |
| Redis 快取 | 加入 Redis 容器 | 模糊搜尋與報價計算速度提升 |
| 圖片 CDN | Cloudflare R2 存商品圖片 | 減輕 NAS 流量 |
| 多用戶系統 | 加入員工帳號、RBAC 權限 | 助理可協助上架 |
| Easy Store API | 串接自動上架功能 | 省去手動操作 |

---

## 附件清單

| 檔案名稱 | 用途 |
|---------|------|
| `docker-compose.yml` | 主容器設定 |
| `nginx.conf` | 反向代理設定 |
| `.env.template` | 環境變數範本 |
| `Dockerfile` | 後端容器建置 |
| `requirements.txt` | Python 套件清單 |

---

> **下一步：** 後端 FastAPI 主程式 `main.py` 的開發，包含商品管理、定價計算引擎、模糊搜尋等核心模組。確認上述部署步驟完成後，即可開始功能開發。
